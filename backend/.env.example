# ===========================================
# Momento Backend - Environment Variables
# ===========================================
# 이 파일을 .env로 복사한 후 실제 값으로 변경하세요
# cp .env.example .env

# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/momento"

# Server
PORT=3000
NODE_ENV=development

# Firebase Admin SDK
# 방법 1: 서비스 계정 키 파일 경로 (권장)
GOOGLE_APPLICATION_CREDENTIALS="./firebase-service-account.json"

# 방법 2: 또는 Firebase 프로젝트 ID만 지정 (ADC 사용 시)
# FIREBASE_PROJECT_ID="your-firebase-project-id"



